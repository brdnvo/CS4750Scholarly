specVersion: "v1"
serviceId: "scholarly"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "scholarly-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
